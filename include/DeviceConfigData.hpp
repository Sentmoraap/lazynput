#pragma once

namespace Lazynput
{
    /// \class DeviceConfigData
    /// \brief The data to get the mappings from config tags
    ///
    /// This class contains all the data needed for a specific device to get the mappings for a given set of
    /// configuration tags.
    /// This is used by DevicedDb, you don't have to use it directly.
    class DeviceConfigData
    {

    };
}
